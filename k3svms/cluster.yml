# curl https://releases.rancher.com/install-docker/19.03.sh | sh
# sudo usermod -aG docker centos

nodes:
  - address: 3.72.104.6
    user: ec2-user
    role:
      - controlplane
      - etcd
      - worker
